This project is a simple setup with CRUD operations for now. This project boilerplate written in django backend and react typescript frontend with JWT authentication and axios configuration.

Work in progress -> Currently completing the frontend. Also, integrating social login with Django allauth.
